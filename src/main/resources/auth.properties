spring.queries.users-query=SELECT email, passwd, active FROM users WHERE email=?
spring.queries.roles-query=SELECT u.email, r.rolename FROM users u INNER JOIN user_role ur ON (u.user_id = ur.users_id) INNER JOIN roles r ON (ur.roles_id = r.role_id) WHERE u.email=?